<p id="notice"><%= notice %></p>

  <% if I18n.locale == :en %>
  <%= @news.title %>
	<%= @news.body %>
	<% else %>
	<%= @news.title_cn %>
	<%= @news.body_cn %>
  <% end %>


<%= link_to 'Edit', edit_news_path(@news) %> |
<%= link_to 'Back', news_index_path %>
